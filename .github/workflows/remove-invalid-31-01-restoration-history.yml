name: Remove Invalid 31-01 Restoration History
on: workflow_dispatch
concurrency: {group: nenova-stock-30-repair, cancel-in-progress: false}
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1.0.3
        with:
          host: 172.233.89.171
          username: root
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          command_timeout: 10m
          script: |
            export PATH=$PATH:/root/.nvm/versions/node/$(ls /root/.nvm/versions/node/ 2>/dev/null | tail -1)/bin:/usr/local/bin
            cd /var/www/nenova-erp
            git fetch origin master && git reset --hard origin/master
            node --env-file=.env.local scripts/remove-invalid-31-01-restoration-history.mjs
