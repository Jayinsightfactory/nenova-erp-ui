# 빌드 출력 — Railway에서 직접 빌드하도록 제외
.next/
out/

# 환경변수 — 보안상 제외
.env
.env.local
.env*.local

# 의존성
node_modules/

# 기타
.DS_Store
*.log

# 로컬 preview 용 임시 파일
.preview/
data/order-mappings.json

# 런타임 업로드 파일 (카카오워크 봇 이미지 등) — 커밋 금지, .gitkeep 만 추적
public/uploads/photos/**
!public/uploads/photos/.gitkeep
