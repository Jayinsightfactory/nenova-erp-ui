# 27차 주차별 매출이익 보고서 대조 결과

검증일: 2026-07-21  
대상 엑셀: `C:\Users\USER\Downloads\매출원가 양식 - 27차_재고수정.xlsx`  
대상 웹: Nenova Web `/sales/profit-report?week=27` (검증 당시 배포 버전 `v1.0.2 · 8ebb55e`)

## 결론

업로드 파일과 웹의 숫자가 다른 이유는 하나가 아니다.

1. 업로드 파일의 첫 시트 제목은 `주차별 매출이익 보고서-26차`로 남아 있지만, 구매현황·포워딩·콜롬비아 원가 시트는 27차 자료다. 즉 파일 내부에 차수 표기와 정적 보고서 캐시가 혼재한다.
2. 웹은 현재 DB의 확정 출고·견적·입고·통관·포워딩·통화마스터·재고 스냅샷을 다시 계산한다. 엑셀은 기존에 저장된 캐시값을 보여준다.
3. 웹의 기존 재고 선택 코드는 `-02` 우선, `-01` fallback으로 고정되어 있어 27-03 이후 마지막 ProductStock 스냅샷을 사용할 수 없었다. 또한 실제 재고 스냅샷과 `StockMaster.isFix` 마감 표시를 혼동하면 27-02처럼 ProductStock은 있는데도 누락으로 판정할 수 있다.

따라서 대조 당시 상태는 숫자와 재고 규칙 모두 100% 일치 상태가 아니었다. 이번 변경에서 재고 선택 규칙은 다음으로 수정했다.

```text
기말 F = 선택 대차수에서 ProductStock 행이 존재하는 세부차수 중 suffix 숫자가 가장 큰 행
기초 E = 같은 매출연도의 전차수(27차라면 26차) 마지막 ProductStock 스냅샷의 같은 규칙
         ※ 01차에서만 연도 경계로 전년도 52차 사용
동일 세부차수 중복 = ProductStock 행 수, 그 다음 가장 큰 StockKey
ProductStock 스냅샷 없음 = 임의 -01/-02 사용 금지 + 검증 오류
StockMaster.isFix = 선택된 스냅샷의 별도 마감 표시·진단값
```

## 합계 대조

엑셀 캐시값과 웹 화면의 총계는 다음과 같다. 금액 단위는 원, 외화는 USD다.

| 열 | 의미 | 엑셀 | 웹 | 웹-엑셀 |
|---|---|---:|---:|---:|
| C | 매출액 | 227,653,408 | 228,876,577 | +1,223,169 |
| E | 기초상품재고액 | 12,578,453.106 | 8,919,590 | -3,658,863.106 |
| F | 기말상품재고액 | 13,229,405.204 | 15,782,222 | +2,552,816.796 |
| G | 매입액(상품+포워딩) | 200,194,632.126 | 180,188,605 | -20,006,027.126 |
| H | 그외통관비 | 10,813,030 | 7,158,726 | -3,654,304 |
| I | 매출원가 | 210,356,710.028 | 180,484,699 | -29,872,011.028 |
| J | 매출이익 | 17,296,697.972 | 48,391,878 | +31,095,180.028 |
| L | 불량금액 | -11,055,272 | -11,055,272 | 0 |
| N | 순수매출 | 239,164,878 | 239,851,695 | +686,817 |
| O | 그외매출 | -456,198 | 80,154 | +536,352 |
| P | 상품 금액(구매) | 133,879,223.465 | 114,285,322 | -19,593,901.465 |
| Q | 구매금액(외화) | 112,727.3 | 112,689.83 | -37.47 |
| S | 포워딩(USD) | 53,051.76 | 52,434.22 | -617.54 |
| T | 포워딩 원화환산 | 66,315,408.661 | 65,903,284 | -412,124.661 |

## 매출 차이의 산식별 원인

현재 양쪽 공통 산식은 `C = N + L + O`다.

- L 불량금액 합계는 정확히 같다: `-11,055,272`.
- N 순수매출은 웹이 `+686,817`이다.
- O 그외매출은 웹이 `+536,352`이다.
- 따라서 `686,817 + 536,352 = 1,223,169`로 C 차이와 정확히 일치한다.
- 웹에는 `기타(미분류)` 행이 생겼고, 그 행에 `N=693,182`, `O=1,186,807`, `C=1,879,989`가 표시된다. 엑셀 첫 보고 시트에는 이 별도 미분류 행이 없다.

즉 매출 C가 틀린 것이 아니라, 확정 출고/견적 원장의 현재 분류와 엑셀에 남은 정적 분류·캐시가 다르다. 엑셀을 정본으로 삼으려면 미분류 품목의 국가·화종 매칭과 N/O 원천 행을 먼저 확정해야 한다.

## 원가 차이의 원인

### 환율

엑셀은 27차 인보이스 환율을 국가별로 저장했다. 기존 웹은 당시 활성 `CurrencyMaster` 기본값을 사용했지만, 이번 수정부터는 차수의 `FreightCost.ExchangeRate` BILL 스냅샷을 구매금액 가중평균으로 먼저 사용하고 스냅샷이 없는 구형 입고만 `CurrencyMaster`로 fallback한다.

| 국가/분류 | 엑셀 환율 | 웹 기본 환율 | 판정 |
|---|---:|---:|---|
| 콜롬비아·태국·에콰도르 등 USD | 1,538.30 또는 1,548.52 | 1,550 | 역사 인보이스 환율과 현재 기본값이 다름 |
| 네덜란드 EUR | 1,766.50 | 1,450 | 차이가 큼 |
| 중국 CNY | 226.66 | 195 | 차이가 큼 |
| 호주 AUD | 1,068.23 | 당시 웹 자동값 없음 | 웹 P가 0으로 남음 |

Q 외화 합계는 거의 같지만, P는 `P=Q×R`이므로 환율 차이만으로도 P와 G가 크게 달라진다. 기존 코드는 현재 `CurrencyMaster`를 과거 차수에도 사용한 것이 문제였고, 이제 `FreightCost.ExchangeRate` 스냅샷을 먼저 사용한다. 27차 엑셀과 100% 숫자를 맞추려면 27차 BILL에 저장된 환율이 실제 인보이스 R열과 같은지 확인하고, 스냅샷이 없는 BILL만 차수 환율을 보정해야 한다.

### 통관비 H

웹 H는 27차 구조화된 중량·트럭·통관 입력 원천을 계산한다. 엑셀 H는 `그외통관비`와 콜롬비아 원가 시트의 기존 금액을 사용한다. 국가별 웹-엑셀 차이는 다음과 같다.

| 분류 | 웹 H | 엑셀 H | 차이 |
|---|---:|---:|---:|
| 콜롬비아 수국 | 2,073,680 | 2,369,680 | -296,000 |
| 콜롬비아 카네이션 | 3,381,879 | 3,069,455.675 | +312,423.325 |
| 콜롬비아 장미 | 470,067 | 972,024.207 | -501,957.207 |
| 네덜란드 | 233,266 | 654,670 | -421,404 |
| 호주 | 0 | 608,410 | -608,410 |
| 태국 | 55,660 | 362,830 | -307,170 |
| 중국 | 429,640 | 1,675,210 | -1,245,570 |
| 에콰도르 | 44,620 | 674,370 | -629,750 |
| 합계 | 7,158,726 | 10,813,030 | -3,654,304 |

따라서 H도 단순 표시 버그가 아니라 엑셀의 정적 통관 입력과 웹 자동원천의 값이 다르다. 엑셀 정본을 유지하려면 파일 업로드/차수 스냅샷 입력 기능이 필요하다.

### 포워딩 S/T

전체 S 차이는 `-617.54 USD`로 작지만, 국가별 배부가 서로 상쇄된다. 예를 들어 콜롬비아 카네이션은 웹이 엑셀보다 `+2,179.763 USD`, 장미는 `-2,446.252 USD`다. 웹은 FREIGHTWISE/중량 기반 구조화 원천을 국가·품목 구성비로 재배부하고, 엑셀은 기존 시트의 분배값을 사용하므로 국가별 행이 일치하지 않는다.

## 재고 검증 결과

기존 코드의 실제 선택 SQL은 다음과 같았다.

```sql
OrderWeek IN (@endWeek, @fallbackWeek)
-- endWeek = 27-02, fallbackWeek = 27-01
```

이 구조에서는 27-03이 확정되어도 27-02를 계속 읽는다. 또한 `StockMaster.isFix=1`을 WHERE에 넣지 않아 미확정 또는 시작재고 마커가 섞일 가능성이 있었다. 웹 화면에서 확인된 스냅샷 표기도 당시 `기말=27-02`, `기초=26-02말`이었다.

수정 후에는 `latestStockSnapshotWeek()`가 `OrderYear + 대차수 prefix + ProductStock 존재`를 제한하고, 세부차수 suffix 내림차순과 ProductStock 행 수·`StockKey DESC`로 마지막 스냅샷을 고른다. 27차 기초재고는 같은 `OrderYear`의 26차 마지막 ProductStock 스냅샷에서 가져온다. ProductStock 스냅샷이 없으면 `STOCK_BEGIN_SNAPSHOT_MISSING` 또는 `STOCK_END_SNAPSHOT_MISSING` 오류를 생성한다. 01차에서만 전년도 52차를 기초재고 후보로 사용한다. `StockMaster.isFix`가 0/NULL이어도 ProductStock이 존재하면 이 보고서의 재고원천 선택을 막지 않고 진단값으로만 노출한다.

### 27-02/29 확정 상태 정정

사용자 확인 기준으로 27-02부터 29차까지 재고는 이미 확정 상태다. 운영 조회에서 `StockMaster.isFix` 표시가 `미확정`으로 보이는 경우에는 실제 `ProductStock` 스냅샷과 별도의 마감 표시 불일치로 기록해야 하며, 이를 이유로 F/E 자동값을 누락시키면 안 된다. 27-02 콜롬비아 장미의 재고현황 합계 279단은 ProductStock 기반 조회와 일치하는 기준값이다.

## 남은 100% 판정 조건

코드 규칙은 수정했지만, 배포 전에는 다음 read-only 검증이 필요하다.

1. DB에서 2026년 27-01, 27-02, 27-03 이후 `StockMaster(OrderWeek,isFix,StockKey)`를 조회해 마지막 `isFix=1` 행을 기록한다.
2. 웹 API의 `stockWeeks.begin/end`가 그 행과 일치하는지 확인한다.
3. 같은 스냅샷의 `ProductStock.Stock`을 nenova.exe 재고현황의 마지막 Stock 열과 품목별로 대조한다.
4. 엑셀을 정본으로 사용할 경우 27차 환율·통관·포워딩 값을 차수 스냅샷으로 저장한 뒤, N/L/O와 함께 재조회해 C/G/H/I/J 총계를 다시 맞춘다.

이번 보고서 조회·검증·코드 수정은 `OrderDetail`, `ShipmentDetail`, `Estimate`, `ProductStock`, `StockHistory`를 쓰지 않는다. 웹 전용 수기 저장은 기존 `WebProfitReport` 경로만 사용한다.
