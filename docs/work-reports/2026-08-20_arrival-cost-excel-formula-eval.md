# 작업 완료 보고 — 도착원가(엑셀)와 도착원가(송이) 불일치

## 메타

| 항목 | 내용 |
|------|------|
| 일시 | 2026-08-20 17:50 |
| 사용자 요청 | 원가(엑셀)과 엑셀 도착원가(송이)가 다른 이유 |
| 브랜치 | fix/arrival-cost-excel-formula-eval |

## 원인

34-2 수국 파일은 `도착원가(송이)=J+K+L` 수식이 있지만 계산값 `<v>`가 비어 있다. Excel은 열면 계산해서 보여주고, 웹은 저장값을 못 읽어 29-1 근사 공식으로 채웠다. 화면도 원 정수로 반올림했다.

## 변경

- 비어 있는 엑셀 수식을 계산해 `도착원가(송이)`와 같게 저장
- 원가(엑셀) 소수 2자리 표시
