# 신라호텔 차수별 결산 통합 설계

## 범위와 원천
- 원본: `C:/Users/USER/Desktop/2026 신라 상반기 입고 손익계산_이사님보고.xlsx` (읽기만).
- 웹 전용 WebRaumPnl / WebRaumPnlItem에 PartnerCode=shilla로 격리. 기존 라움+초이문 공유 단가 저장 범위를 넓히지 않는다. 신라 단가는 별도로 표시/수정한다.
- 주문/분배/입고/재고/견적/ERP 손익 원장은 전부 보존. 새로운 주문등록 또는 전산 분배 맞춤 기능은 신라에서 활성화하지 않는다.
- 저장 식별자: OrderYear + MajorWeek + PartnerCode. 엑셀 차수를 보존하고 전산 차수 메모는 표시만 한다. 전산 차수 연결은 확인 전 추측하지 않는다.
- 신라 원본 단가를 다른 업체의 학습단가/도착원가로 덮거나 WebRaumCostPrice에 학습하지 않는다.

## 원본 분석 결과
- 38개 시트 중 단일 차수 시트 35개. 검토리포트/결산/26차~30차(7월)(은 중복 집계 방지를 위해 결산 입력 제외.
- 단일 차수명은 5차ㅇㅋ, 11차까지끝, 22차까지 금액만, 31차8월도 허용. 범위 시트는 제외.
- 행2 헤더를 이름으로 인식: 품명/칼라/입고수량/매입단가/매입액/매출단가/매출액/이익/이익율/네노바이익(%)/미우이익(%).
- 22차 매출액=I열(부가세포 G/H 제외), 29/30차 이익=I열(중간 빈 열). 고정 위치 파싱 금지.
- 첫 합계 행까지만 품목. 그 아래 전산 참고내역은 이중 합산하지 않는다.
- 매입액=수량*매입단가, 매출액=수량*매출단가, 이익=매출액-매입액. 원본 cached 값과 행/합계 모두 대조한다. 0은 값이고 null/수식 오류는 누락이다.
- 1~26차 원본 배분율 60:40, 27~35차 80:20. 원본 헤더/수식으로 검증하며 기존 DEFAULT 80을 일괄 주입하지 않는다.
- 31차8월 E10=SUM(E3:E8)에서 E9 266400원 누락; G10은 3:9, H10은 3:9, J10/K10은 3:8. 합계 불일치는 실패로 표시/저장 차단. 원본 수정 금지.
- 35차 A8은 공백, B8=화이트, C8=324, D8=11233, F8=13000. 호접으로 추측 금지. 원문행 보존하며 품목 확인 필요로 표시/저장 차단.
- 33차 B7=태국샘플, A7 빈칸. 이름은 태국샘플로 보존하되 단위는 미확인(임의 단/송이 환산 금지), 원본 금액은 합산.
- 24차 제목은 전산25차, 25차 제목 전산26차, 26차 제목27차 전산. UI와 원본 비고에 이 차이 표시. 자동 ERP 조회/품목매칭을 사용하지 않는다.

## 완료 기준
1. 신라 선택·차수별 목록/상세/매입단가·엑셀 출력 진입 가능.
2. 별도 신라 업로드 parser와 명시 연도 입력, 미리보기 후 명시 저장. 확인 실패 차수는 표시하고 저장 금지; 사용자 선택한 정상 차수만 원자 저장 가능.
3. 기존 라움/초이문 조회·공유 단가 동시수정·사입 연결 회귀 없음.
4. 품목/단위 정확 일치 전에는 ERP ProdKey 자동 확정 없음. 신라 단가를 기존 두 업체에 전파하지 않음.
5. 실제 원본 35차수 전체 행/합계 검증 결과와 미해결 사항 보고. 테스트 통과와 운영 자료 저장 완료를 구별.
6. 기본 UI 1920x1080, Layout 중복 금지, 스크롤·입력 폭·오류 확인 가능.

## 구현 위임 규칙
APPROVAL_FREE_CHILD. 허용 P0_LOCAL만. 외부 호출/운영 DB/secret/push/병합/배포 금지. 읽기·편집은 배정 파일만. 추가권한은 NEEDS_MAIN_PREFLIGHT. 원본 Excel 변경 금지. 파일 편집은 apply_patch 사용.

## 사용자 확인 (2026-09-07)
- 35차 A8 품목은 호접 화이트로 확인됨. 원본 SHA256 `1ce46b834326038680c2768b04431637a6e09f1bea2bd45b5e5ce34badf6b7e4` 일치 시에만 입력용 복사 데이터에서 호접(8스팀)으로 보완. 원본 변경 없음.
- 차수는 엑셀 기준 보존, 전산 차수는 메모로 분리하도록 승인.
- 31차 염색 호접 포함 매입액 6,834,613원, 이익 2,468,949원의 80:20 배분 승인. 위 원본 해시 일치 시만 E10/J10/K10 입력용 합계를 각각 6,834,613/1,975,159.2/493,789.8로 보완하고 원값/승인 내역 기록.
- 15차 염색 호접 48개는 무상입고로 사용자 확인. 매입단가 0원을 명시하고 매출/손익에 포함.
- 7월 이전 60:40, 7월부터 80:20 사용자 재확인. 30차 장미 두 행에 남은 60:40 금액은 원본 헤더와 승인 기준인 80:20으로 보정. 원본 매입/매출/전체 이익은 보존.
- 원본에 수량 총합이 없는 것은 서로 다른 단위의 합산을 요구하지 않는 정상 양식. 행별 수량/단가/금액과 금액 합계/배분액 합계는 모두 검증.

## 검증 진행 (운영 반영 전)
- 원본 38시트 중 35차수 186행을 대상으로 승인 보정 적용 후 행/합계 검증 실패 0건.
- 명시 연도, 색상 포함 품목 식별, 원본 단위(단-5스팀/8스팀) 보존. 웹 저장용 price/supply/costPrice 필드까지 연결 검사.
- 무상입고0/누락null, 60/80/0% 출력, 같은 차수 다른 연도, 파일해시 불일치 보정금지, 미검증 차수 선택차단 회귀검사.
- 역할: Claude Sonnet parser 초안, Codex terra 구조/화면, Codex sol 통합·저장 위험 검토. 외부 쓰기와 배포는 메인만 담당.
