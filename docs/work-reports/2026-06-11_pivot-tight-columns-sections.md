# 작업 완료 보고 — Pivot 타이트 셀 + 열 그룹 + 04.출고

## 메타

| 항목 | 내용 |
|------|------|
| 일시 | 2026-06-11 |
| 범위 | 셀 폭, 열 영역 DnD, 구분 연동, 필터값, 04.출고 |

## 변경 요약

1. **타이트 셀** — `.tbl-pivot` (max-content, padding 1px 4px, minWidth 제거)
2. **열 영역** — `columnZone` state: 구분(01~05)·지역·비고·거래처/농장 드래그 가능
3. **구분 연동** — 열에 `02.주문`/`거래처명` → 주문열, `03.입고`/`농장명` → 입고열 자동 활성
4. **04.출고** — `confirmedOut` (확정 출고량) 섹션 추가
5. **필터값** — 거래처명/농장명/지역/비고 distinct 보정, 주문·입고 매칭 필터

## 사용법

- **열** 영역에 `01.전재고`~`05.현재고`, `지역`/`비고`/`거래처명`/`농장명` 드래그
- 하단 **구분** 칩 ↔ 열 영역 동기화
- `04.출고` = 확정 출고 합계 (`ShipmentDetail` isFix=1)
