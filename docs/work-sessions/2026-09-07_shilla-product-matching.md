# 신라 품목 연결·차수별 매입단가 통합

## 사용자 요청

“신라도 품목 매칭 가능하게 해주고, 차수별 매입단가관리에 같이 매칭 되었으면해.”

## 적용할 답변/설계

- 신라 저장행에서 전산 품목 검색·선택·연결 해제를 제공한다.
- 같은 전산 품목과 같은 단위는 차수별 매입단가 관리의 한 품목 행에서 함께 비교한다.
- 라움·초이문 공통 단가와 신라 별도 단가는 독립 저장한다. 세 업체 공통 가격으로 확대하지 않는다.
- 원본명·색상·단위·수량·금액·이익배분율을 연결 때문에 변경하지 않는다.
- 전역 매칭 학습과 ERP 주문·분배·재고·견적 원장은 변경하지 않는다.

## 실제 확인

사용자가 저장한 신라 2026년 35차 6개 품목을 로그인 화면에서 읽었다. 매입 합계 10,250,892원, 매출 합계 13,145,120원이다. 단위가 다른 품목은 임의 환산하지 않는다. 실제 사용자 자료를 시험 목적으로 변경하지 않았다.

## 역할/자료

- 설계·최종 검토: 고성능 작업자. 범위 확정 구현: 중간 비용 작업자, API/순수 함수와 화면을 분리.
- 외부 쓰기·운영 반영은 메인만 수행한다.
- 설계: `docs/work-reports/2026-09-07_shilla-product-matching-design.md`
- 사전 근거: `docs/work-reports/2026-09-07_shilla-product-matching-preflight.md`
- 상태: 기능·안내 문구 운영 반영 완료, 운영 조회 검증 완료.

## 구현·로컬 검사

- 신라 행 전용 품목 연결 저장, 활성 품목 재확인, 행/차수 충돌 차단과 재업로드 연결 보존을 구현했다.
- 같은 전산 품목·단위의 공통 단가와 신라 단가는 한 행의 별도 입력칸에 표시한다. 무상입고 0원·다른 단위·미매칭 품목은 보존한다.
- 연결 성공/해제/no-op, 2025·2026 교차연도, 모든 원본값 변경, 부모 저장 실패 시 되돌림, 가격만 바뀐 경우의 독립성, 신라 전용 셀 격리와 중복 결산 차단을 실행형 검사했다.
- 실제 React 렌더링에서 공통/신라 입력 2개, 0원, 입력 중 매입액 계산, 음수 안내, 저장 중 비활성 상태를 검사했다.
- `verify:erp-change` 전체 성공(103페이지 빌드 포함). 최종 안내 보완 후 `test:raum-pnl`, 재빌드, 신규 API 1개 포함 쓰기 보호, 계약 42개 범위 검사와 dnSpy 근거 검사도 성공했다. 고성능 최종 검토에서 미해결 P1/P2 없음. 운영 반영은 이어서 메인이 수행한다.

## 운영 반영·읽기 전용 확인

- 변경 #524, 운영 반영 b441170f, Cafe24 34076561868 성공(실브라우저 검사 포함).
- 로그인된 신라 35차 6개 행의 연결 버튼과 실제 전산 검색을 확인했다. 쉬머 검색에서 길이·국가별 후보가 구분된다. 임의 선택·저장하지 않았다.
- 통합 단가 화면 35개 차수/140개 품목 조회 정상. 신라 쉬머 35차 10,800원, 16개, 매입액 172,800원 및 판매액 191,520원으로 원본과 일치한다. 미연결 신라는 별도 행이며 선택 연결 후 같은 품목/단위로 합쳐지는 실행 검사는 로컬에서 통과했다.
- 두 운영 화면 console error/warn 0건. CSS viewport 1920×1080 및 1000×800에서 전체 페이지 가로 넘침 없음, 저장 버튼 접근 가능. 기존 Chrome 확대율은 80%로 남아 있어 100% 확대 검증과 동일하다고 주장하지 않는다. 임시 viewport는 원복했다.
- 구 안내문 한 곳을 정정한 #525는 검사 46초 통과 후 병합(3a95150b). 기능/계산 변경 없음. 운영 설치 34077076779 성공(2분 30초, 실브라우저 검사 포함). 새 로그인 탭에서 3a95150b 및 수정 안내를 확인했다.
- 운영 품목 연결/단가 저장 시험은 하지 않았으며 사용자 원본 수량·금액·ERP 데이터는 변경하지 않았다.

## 후속: 한글 검색 누락

- 사용자: 한글 품목명으로 검색되지 않음.
- 원인: 기존 item-mapping 검색이 한글 별칭 확장 전에 SQL에서 영문 후보를 제외. 운영 그라시오사 0건, 안시리움 검색에는 Graciosa #181 존재를 확인.
- 수정: 신라 검색만 공통 products/search GET로 교체, 조합 Enter 차단, 확정 Enter 현재 입력값 사용, 늦은 응답 차단 유지, 0건/오류 안내 분리. 라움 API 및 신라 저장 API는 변경하지 않았다.
- 전체 verify:erp-change/103페이지 빌드, 신라 집중검사와 dnSpy evidence 통과. 고성능 검토 GO.
- #526 자동검사48초 성공 후 ec13ef34 반영. 설치 34083045020 성공. 운영 그라시오사/Graciosa 검색에서 #181 등 실제 후보 표시, 오류0건. 실제 품목 연결은 하지 않음. 세부근거: ../work-reports/2026-09-07_shilla-korean-search.md.
