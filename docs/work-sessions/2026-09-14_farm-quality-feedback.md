# 농장 불량·피드백 — 2026-09-14

## 사용자 요청과 합의

- 영업수입 불량차감의 수입부 확인 품목·농장 기반 주차별 그래프와 피드백 추적.
- 거래처명·금액·차감 처리 제외. 미답변/기한초과/답변/관찰/개선/재발 상태 색상 및 필터.
- 영업·수입 코멘트는 로그인 담당자·부서·시각을 기록하며 덮어쓰지 않는 이력.
- 코멘트와 농장 답변은 구별. 수입부/관리자가 명시적으로 답변 처리.
- 1920×1080 100% 기준, 좁은 화면에서도 필터·상세·저장 접근 가능.

## 구현

- `/sales/farm-quality`, 기존 불량차감 화면 바로가기 및 공통 메뉴.
- 원본 불량 자료는 읽기 전용. 단위별 분리, OriginalQuantity 우선으로 이월 차감량 중복 집계 방지.
- 대차수 단위 그래프, 피드백은 해당 연도 전체 표시. 입고모수가 없어 불량률을 추정하지 않음.
- WebFarmQualityCase/Event 신규 저장소만 쓰기. 동일 요청 재시도 멱등, 상태 변경 버전 검사.
- 적용 차수 이후 불량은 재발 의심 표시. 담당자가 확인 후 재발 확정, 자동 개선 판정 없음.
- ERP 원장, 차감 등록/금액/견적/출고/재고 불변.

## 검증 진행

- 실제 로컬 dnSpy CLI FormSalesDefectView.GetData 확인: 별도 financial Estimate 조회 경로.
- ERP 계약 전체, 독립 순수 정책 및 트랜잭션 mock 테스트 통과.
- 프로덕션 빌드 통과. 최종 UI 변경 이후 재빌드/브라우저 검증 진행 중.
- 운영 스키마 읽기 전용 probe, PR/배포/운영 smoke는 아직 완료하지 않음.
- 개발 모드에서 기존 instrumentation → mssql의 crypto 해석 오류가 있어 프로덕션 모드로 UI 검증.

## 후속 갱신

검증·배포 결과와 PR 링크를 아래에 기록. 완료 전 운영 반영이라고 보고하지 않는다.

- PR: https://github.com/Jayinsightfactory/nenova-erp-ui/pull/568
- 운영 read-only probe run 34796614036 성공: 필요한 원본 컬럼 누락 없음.
- 2026년 원본 418행, 수입확인/농장/품목 조건 대상 241행. 원장 쓰기 없음.
- 1920×1080 / 760×1080 / 390×1080 fixture 브라우저 테스트 통과.
- 저장 실패 내용 보존, 동일 requestId 재시도, 미답변 필터, 화면틀 검증 통과.
- 추가 그래프 최소폭 보정 후 최종 빌드와 브라우저 재검증 통과.
- PR 568 ERP Contract Guard 및 운영 source probe 통과. 병합/배포 결과는 아래 추가 기록.

## 최종 결과 — 완료

- PR 568 병합 및 배포 run 34797037712 성공.
- SQL uniqueidentifier 대문자/웹 UUID 소문자 차이를 통일하여 저장 후 선택 유지.
  대문자 SQL GUID 재시도 회귀 테스트 추가. PR 569 병합.
- 최종 master `159e17d28bba8fe371b9e44214202c52053e6a99`.
- 최종 Cafe24 배포 run **34797221463 성공**, 서버 migration/read probe/build/기존 hydration smoke 모두 통과.
- 별도 운영 로그인 읽기 전용 smoke: 145 농장·품목·단위 그룹 조회, 피드백 신규 상태 0건.
  1920×1080 실제 그래프/메뉴틀/가로 넘침/브라우저 오류 검사 통과.
- 정책/트랜잭션 mock 테스트로 저장·권한·재시도·교차연도 검증.
  운영 확인에서는 테스트 피드백을 저장하지 않았고 ERP 원장도 수정하지 않음.
- 화면 진입: `/sales/farm-quality?popup=1`; 기존 영업수입불량차감 바로가기.
- 사용: 불량 그래프 → 품목·농장 선택 → 피드백 건 만들기 → 부서 코멘트/농장 요청·답변/개선 이력.
- 운영 농장/단위 표기는 원본을 유지하며 박스·단·스팀 등을 임의 환산·병합하지 않음.
- UI fixture/운영 캡처는 해당 작업 폴더 `outputs/farm-quality/`에 보관.
- 원래 작업폴더의 AGENTS.md·기존 세션 메모·outputs 변경은 보존.
