# 전산 피벗 자동 조회·개인 즐겨찾기

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-09-14 |
| 화면 | /stats/pivot 전산 피벗 |
| 원장 부작용 | ERP 원장/SQL/SP 보존. 명시 저장 시 기존 UserFavorite 개인 화면 조합만 변경 |
| 배포 | 검증 후 기록 |

## 고정 결정

- 페이지 진입과 유효한 연도·세부차수 변경은 자동 조회한다.
- 행·열·값·필터 드래그와 좌클릭 메뉴는 동일 배치 경로를 사용한다. 이동 시 원본을 재조회하지 않고 재집계한다.
- 즐겨찾기는 서버의 현재 사용자 목록이다. 이름으로 새 저장/선택 덮어쓰기/불러오기/삭제한다.
- 조합을 불러와도 선택 차수는 바꾸지 않는다. 수량·매입단가 같은 원본 자료는 저장하지 않는다.
- 마지막 화면은 이 브라우저의 인증 사용자별 키로 저장한다. 이름 있는 즐겨찾기는 같은 계정의 다른 PC에서도 조회 가능하다.
- 기존 사용자 구분 없는 localStorage 키는 자동 승계하지 않는다.

### 1. 요청

**Q.** 새로고침 없이 자료가 보이고 사용자별 즐겨찾기 조합이 저장되며, 필드를 행·열로 옮길 때 실제 피벗이 바뀌어야 한다.

**A.** EXE의 FormQuantityPivot_Load → SetCombo/GetData를 확인했다. 기존 웹은 버튼 조회만 연결되어 있었고 drag/drop은 없었다. 자동 조회, 개인 즐겨찾기, 모든 영역 이동과 접힌 그룹 해제를 구현한다. 검증·배포 결과는 완료 시 갱신한다.

## 작업 보고서

[기준·부작용·검증](../work-reports/2026-09-14_pivot-auto-favorites.md)

## 로컬 산출물

outputs/ 아래 빌드·화면·fixture 다운로드는 커밋하지 않는다. 이전 작업의 미커밋 배포 기록 두 파일은 보존한다.
