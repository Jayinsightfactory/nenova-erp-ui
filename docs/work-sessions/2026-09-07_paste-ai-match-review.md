# 붙여넣기 업체·품목 AI 최종 검증

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-09-07 |
| 화면 | 붙여넣기 주문등록 / 공용 parse-paste |
| 원장 부작용 | 주문·분배·재고·견적·매출 원장 쓰기 없음 |
| 상태 | PR #531 병합·운영 배포 성공. 실분석에서 발견한 후보 누락은 PR #534 후속 검증 중 |

## 고정 결정

- 사용자가 API 비용을 허용하고 업체·품목 매칭 개선을 요청했다.
- 기존 Claude API의 문장 파싱 뒤 최종 후보 검증을 추가한다. 실제 활성 후보 키만 선택.
- 업체명 완전일치는 지역명/과거 별칭보다 우선. 정확 저장 별칭은 유지하고 부분 별칭·AI 제안 키는 비교 후보다.
- 품목 후보는 기존 검색 결과/현재 선택을 사용하고 국가·화종·명시 길이 불일치를 제거한다.
- AI 누락/중복/목록 밖 키/확신0.9미만/실패는 확인 필요, 원문 수량·단위·연도·차수·취소/추가 보존.
- 서버 검증 결과는 미매칭이어도 브라우저 저장값이 덮어쓰지 못한다.
- 품목 학습 저장은 Git 추적 seed와 분리된 runtime JSON. 배포는 기존 값 보존 및 이전 실행 빌드의 구파일 쓰기도 runtime 링크로 연결한다.

## Q&A

### 1. 업체와 품목 매칭이 바뀌었나
Q. 양재동이 다른 업체, 서부꽃집이 서부청과로 나온다.
A. 운영 read-only 조회상 양재동686/서부꽃집671 별칭은 정상. 브라우저 캐시가 서버 결과를 덮는 경로와 Git 추적 품목 저장파일의 배포 초기화 위험 확인. 특정 과거 요청의 오선택 원인을 로그만으로 확정한 것은 아니다.

### 2. API로 정확도 보완 시작
Q. 품목도 API 분석을 추가해서 보완하라.
A. 기존 Anthropic API로 후보별 재검증을 구현했다. 고객/품목 키만 검증 선택하고 주문등록 실행은 분리했다. 근거를 미리보기에 표시하며 사용자 수정은 기존 검색/저장 API 유지.

## 검증

- dnSpy FormOrderAdd 조회/저장 이벤트 확인, 인증된 master/별칭 조회로 대상 키 대조.
- 신규 실행형 fixture: 고객 충돌, 품목국가/길이, AI 응답 오류, 실패, 원문/교차연도 보존, stale cache, 배포 seed 변경 뒤 runtime 보존.
- `test:erp-contract`, `test:erp-manifest -- --changed-from origin/master`, `guard:erp-writes`, `test:nenova-dnspy-evidence`, `build` 통과.
- 운영 분석 및 1920×1080 화면 스모크는 배포 후 실시. ERP 등록 테스트는 하지 않는다.

## 운영 검증 추가 기록

- PR #531 merge `1a77f438`, Cafe24 run `34103558199` 성공(계약/SSH/실브라우저 hydration).
- 별도 1920×1080 브라우저에 잘못된 localStorage 매칭을 넣은 분석에서 양재동686/부산 서부꽃집671 정상 확인.
- 품목은 `콜장미 핑크몬디알`이 공통 검색의 후보에서 누락돼 확인 필요로 차단됐다. AI가 잘못된 Absolute Pink로 강제 선택하지 않았음.
- 후속 PR #534는 공통검색 외에도 전산 품명의 한글→영문 품종 토큰 검색을 수행해 후보를 보완한다. 원문 규격도 검증 API에 전달한다.
- smoke는 인증/분석 POST만 허용하고 나머지 쓰기는 브라우저 요청 계층에서 차단해 실제 분배 및 사용자 작업권을 보존한다.
- 배포·최종 재검증 결과는 PR #534 검증 댓글도 확인한다. outputs/paste-ai-browser-smoke.cjs 및 png는 비커밋 진단 산출물이다.

## 이어받기

이 문서와 `docs/work-reports/2026-09-07_paste-ai-match-review.md`, 신규 계약을 읽고 PR/배포 상태 및 실제 분석 결과를 확인한다. 완료 전 정확도100% 또는 운영 적용 완료라고 말하지 않는다. outputs/는 진단 산출물이므로 커밋하지 않는다.
